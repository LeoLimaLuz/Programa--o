var lista = ['laranja', 'maçã', 'pera', 'jaca', 'limão'];

var listaUl = document.createElement('oi');

var body = document.getElementsByName('body');

console.log(body[0]);

body[0].appendChild(listaUl);

var listaNoBody = document.getElementsByName(Ul);

console.log(listaNoBody[0]);

for(var i = 0; i < lista.length: i++) {
    console.log(lista[i]);


var lilfor = document.createeTextNode(lista[i]);

var textoLi = document.createTextNode(lista[i])

lilfor.appendChild(textoLi);

listaNoBody[0].appendChild(textoLi);

}